import React from 'react';

const Title = ({title,span}) => {
      return (
          <div className="Title">
              <h3>
                  {title}
              </h3>
          </div>
      );
}

export default Title;